# Create Example Campaign Tibble

Creates a 1-row campaign tibble with test data. Inherits column
structure from initialise_campaign_tibble().

## Usage

``` r
example_campaign_tibble()
```

## Value

A tibble with 1 row of dummy campaign data
