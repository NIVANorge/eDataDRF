# Controlled vocabulary for PROTOCOL_NAME and PROTOCOL_TYPE (sampling protocols)

Returns sampling protocol options as a tibble with Protocol_Type,
Short_Name, and Long_Name columns.

## Usage

``` r
sampling_protocols_vocabulary()
```

## Value

A tibble with sampling protocol options
