# Create Example Sites Tibble

Creates a 2-row sites tibble with test data. Inherits column structure
from initialise_sites_tibble().

## Usage

``` r
example_sites_tibble()
```

## Value

A tibble with 2 rows of dummy site data
