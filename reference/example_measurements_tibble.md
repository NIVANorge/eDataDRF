# Create Example Measurements Tibble

Creates a 3-row measurements tibble with test data. Inherits column
structure from initialise_measurements_tibble().

## Usage

``` r
example_measurements_tibble()
```

## Value

A tibble with 3 rows of dummy measurement data
