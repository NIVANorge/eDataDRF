# Initialise Samples Data Tibble

Creates an empty tibble with the standardised column structure for
sample data. Samples represent individual collections from sites with
temporal, spatial, and methodological information linking sites,
parameters, and compartments.

## Usage

``` r
initialise_samples_tibble()
```

## Value

A tibble with 0 rows and standardised sample columns
